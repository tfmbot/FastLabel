# FastLabel
FastLabel — a fast image labeling tool that uses your trained YOLO model to prefill boxes, so you can label datasets quickly, edit easily, and export clean YOLO files without extra hassle.
